{% extends "base.html.twig" %}

{% block body %}
	<div class="img-fluid">
		<img class="card-img-top" src="/assets/blog/images/{{ article.image }}" alt="{{ article.image }}" width="100%">
	</div>
	<p class="date">Ecrit le
		{{ article.createdAt | date("m/d/Y") }}
		à
		{{ article.createdAt | date("H:i") }} par {{ article.author }}</p>
	<h2 class="title p-1">{{ article.title }}</h2>
	<p class="content text-justify">
		{{ article.content | raw }}
	</p>
{% endblock %}
